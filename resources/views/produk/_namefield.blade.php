<div class="d-flex flex-column">
    <span class="font-weight-bold text-blue">{{ $data->nama }}</span>
    <span><strong>SKU</strong> : {{ $data->sku }}</span>
    <span><strong>Brand</strong> : {{ $data->brand->nama }}</span>
    <span><strong>Unit</strong> : {{ $data->unit->nama }}</span>
</div>
